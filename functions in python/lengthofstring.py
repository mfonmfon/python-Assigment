def printed_name(name):
    
    for counter in name:

      return counter
      counter += 1
	
      print(printed_name(len(name))






def listofwords(longest):
	word = " "

	for i in longest.length:
		word += 1
		return word
print(listofwords(["welcom", "out", "weather", "mobile", "breakfast", "journey"]))



def addingdstring():
	

	for i in range(len(userinput)):


	userinput = input("Type a word: "))

		if userinput == "ly":
			return add("ing")
		else:
			return add("ing")																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																
print(addinstring(userinput))





def(){

	if len(text) < 2:
    return ""
  else:
    return text[:2] + text[-2:]

print(manipulate_string('semicolon'))  # Output: seon
print(manipulate_string('on'))         # Output: onon
print(manipulate_string('o')) 
	}
  
